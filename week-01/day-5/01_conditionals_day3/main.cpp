#include <iostream>
#include <string>

void yesorno (string) {
    std::string y;
    std::string n;

    if (yesorno = y) {
        bool
    }

}

int main(int argc, char* args[]) {
    std::cout << "Are you hungry? Y/N" << std::endl,
    std::cin >> yesorno;

    }
    bool thirsty;
    bool hungry = false;

    if(thirsty && hungry) {
        std::cout << "Lunch time!" << std::endl;
    } else if (thirsty || hungry) {
        std::cout << "Snack time!" << std::endl;
    } else {
        std::cout << "Adventure time!" << std::endl;
    }

    return 0;
}